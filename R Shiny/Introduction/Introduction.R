#Example
library(shiny)
runExample("01_hello")

#app.R has 3 components (a user interface object, a server function, a call to the shinyApp function)

#ui object controls the layout and appearance of the app
#server function contains the instructions that your computer needs to build your app
#shinyApp function creates Shiny app objects from an explict UI/server pair



runApp("Introduction")

#Change the title from "Hello Shiny!" to "Hello World!"

#Set the minimum value of the silder bar to 5

#Change the histogram border color from whilte to orange
